[project]
name = "nimbusos-sdk-sandbox"
version = "0.1.0"
description = "External-consumer sandbox for the NimbusOS SDK."
requires-python = ">=3.12,<4.0"
dependencies = [
    "nimbusos-sdk==0.1.0",
]
